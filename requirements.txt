numpy>=1.24
openpyxl>=3.1
ezdxf>=1.1
flask>=3.0

# Noyau CAO OpenCascade. Version verrouillee : c'est celle avec laquelle toute
# la chaine a ete validee (balayage, exports STEP/STL/BREP, lecture STEP,
# tesselation). Paquet volumineux, environ 300 Mo au telechargement.
# Requis uniquement pour la 3D : inspect, calibrate et plan tournent sans.
cadquery==2.8.0

# Fenetre native au lieu du navigateur. Facultatif.
# pywebview>=5.0
